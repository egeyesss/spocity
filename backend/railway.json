{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && gunicorn spocity.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "healthcheckPath": "/api/health/",
    "restartPolicyType": "ON_FAILURE"
  }
}
